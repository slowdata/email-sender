{
  "name": "emailsender",
  "version": "1.0.0",
  "description": "simples script to send email",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "cross-env NODE_ENV=development nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "RD",
  "license": "MIT",
  "dependencies": {
    "cross-env": "^5.2.0",
    "nodemailer": "^5.1.1"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  }
}
